# 🚀 Git & GitHub — Гайд для команды

## 📋 Содержание
1. [Установка и настройка](#1-установка-и-настройка)
2. [Жизненный цикл работы с Git](#2-жизненный-цикл-работы-с-git)
3. [Как называть коммиты](#3-как-называть-коммиты)
4. [Работа с ветками](#4-работа-с-ветками)
5. [Работа с GitHub](#5-работа-с-github)
6. [Pull Request и совместная работа](#6-pull-request-и-совместная-работа)
7. [Конфликты (что делать если что-то пошло не так)](#7-конфликты)
8. [.gitignore — что не пушить](#8-gitignore)
9. [Шпаргалка команд](#9-шпаргалка-команд)

---

## 1. Установка и настройка

### Установка Git
1. Скачай Git для Windows: https://git-scm.com/download/win
2. Установи с настройками по умолчанию
3. После установки открой **Git Bash** (правая кнопка мыши → Git Bash Here)

### Первоначальная настройка
Выполни эти команды один раз (используй свои имя и email):

```bash
# Укажи свое имя (будет видно в коммитах)
git config --global user.name "Твоё Имя"

# Укажи свой email (тот же, что на GitHub)
git config --global user.email "твой@email.com"

# Настрой редактор по умолчанию
git config --global core.editor "nano"

# Настрой цветной вывод
git config --global color.ui auto

# Проверь настройки
git config --list
```

### Настройка SSH для GitHub

SSH — это способ безопасного подключения к GitHub. Настраивается один раз, работает вечно.

#### Шаг 1: Генерация SSH-ключа
```bash
# Замени на свой email с GitHub
ssh-keygen -t ed25519 -C "your_email@example.com"
```
- Когда спросит "Enter file in which to save the key" → жми **Enter** (сохранит в `~/.ssh/id_ed25519`)
- Когда спросит passphrase (пароль) → можешь нажать **Enter** (оставить пустым) или придумать пароль

#### Шаг 2: Запуск SSH-агента
```bash
# Запусти SSH-агент
eval "$(ssh-agent -s)"

# Добавь ключ в агент
ssh-add ~/.ssh/id_ed25519
```

**Если на Windows возникла ошибка "Access is denied":**
```bash
Get-Service ssh-agent | Set-Service -StartupType Manual
Start-Service ssh-agent
ssh-add ~/.ssh/id_ed25519
```

#### Шаг 3: Скопируй публичный ключ
```bash
# Выведи ключ в терминал
cat ~/.ssh/id_ed25519.pub
```
Скопируй всё, что вывелось (начинается с `ssh-ed25519 ...`)

#### Шаг 4: Добавь ключ на GitHub
1. Открой https://github.com/settings/keys
2. Жми **New SSH key**
3. **Title:** `Мой компьютер` (или любое название)
4. **Key:** вставь скопированный ключ
5. Жми **Add SSH key**

#### Шаг 5: Проверь подключение
```bash
ssh -T git@github.com
```

Если видишь это первый раз:
```
The authenticity of host 'github.com' can't be established.
Are you sure you want to continue connecting (yes/no)?
```
Печатай `yes` и жми Enter.

Если всё OK, увидишь:
```
Hi <твой-username>! You've successfully authenticated, but GitHub does not provide shell access.
```

✅ **Готово! SSH настроен.**

---

## 2. Жизненный цикл работы с Git

### Схема работы
```
Рабочая директория → git add → Staging Area → git commit → Локальный репозиторий → git push → GitHub
       ↑                                                           ↓
       └──────────────────── git pull ──────────────────────────────┘
```

### Базовый workflow (каждый день)

#### Шаг 1: Клонируй репозиторий (один раз)

**⚠️ ВАЖНО: Используй SSH ссылку, НЕ HTTPS!**

На GitHub при копировании ссылки выбери **SSH**, а не HTTPS:

```bash
# ✅ ПРАВИЛЬНО (SSH)
git clone git@github.com:username/project.git

# ❌ НЕПРАВИЛЬНО (HTTPS)
git clone https://github.com/username/project.git
```

После клонирования:
```bash
# Перейди в папку проекта
cd project

# Проверь что используется SSH
git remote -v
# Должно быть: origin  git@github.com:username/project.git
```

**Если случайно склонировал через HTTPS:**
```bash
# Поменяй на SSH
git remote set-url origin git@github.com:username/project.git

# Проверь
git remote -v
```

#### Шаг 2: Перед началом работы — ВСЕГДА делай pull
```bash
# Забери последние изменения с GitHub
git pull origin main
```
**Почему важно:** Кто-то из команды мог запушить код, пока ты не работал. Pull забирает эти изменения.

#### Шаг 3: Создай свою ветку для задачи
```bash
# Создай ветку и переключись на неё
git checkout -b feature/название-задачи
```

#### Шаг 4: Работай над кодом
Пиши код, создавай файлы, редактируй...

#### Шаг 5: Проверь статус (что изменилось)
```bash
# Посмотри какие файлы изменены
git status
```
**Красные файлы** — изменены, но не добавлены в коммит  
**Зелёные файлы** — добавлены и готовы к коммиту

#### Шаг 6: Добавь изменения
```bash
# Добавить все изменённые файлы
git add .

# Или добавить конкретный файл
git add filename.js

# Или добавить несколько файлов
git add file1.js file2.css
```

#### Шаг 7: Сделай коммит
```bash
# Зафиксируй изменения с сообщением
git commit -m "feat: добавил форму логина"
```

#### Шаг 8: Запуш в GitHub
```bash
# Отправь ветку на GitHub
git push origin feature/название-задачи
```

#### Шаг 9: Создай Pull Request
Иди на GitHub → увидишь кнопку **Compare & pull request** → создай PR → жди ревью

---

## 3. Как называть коммиты

### Формат коммита
```
тип: краткое описание что сделано

Примеры:
feat: добавил кнопку "Войти"
fix: исправил ошибку в валидации формы
refactor: переписал функцию getUser на async/await
docs: обновил README с инструкцией
style: исправил отступы в header.css
```

### Типы коммитов
- **feat** — новая функция/фича (добавил что-то новое)
- **fix** — исправление бага
- **refactor** — переписал код (работает так же, но код лучше)
- **docs** — изменения в документации
- **style** — форматирование, отступы (НЕ CSS стили!)
- **test** — добавил/исправил тесты
- **chore** — рутина (обновил зависимости, настройки)

### Правила
✅ **Хорошие коммиты:**
```
feat: добавил страницу регистрации
fix: исправил падение при пустом email
refactor: вынес валидацию в отдельную функцию
```

❌ **Плохие коммиты:**
```
update           # что обновил?
фикс             # что фиксил?
asdasd           # 🤦
changes          # какие изменения?
```

### Когда делать коммит?
- ✅ Сделал логически завершённую часть (кнопка работает, функция написана)
- ✅ Перед обедом/концом дня (сохрани прогресс)
- ✅ Перед экспериментами (можно откатиться)
- ❌ НЕ делай один коммит на весь день работы
- ❌ НЕ коммить код, который не работает (если это не WIP)

**Золотое правило:** Один коммит = одна логическая задача

---

## 4. Работа с ветками

### Что такое ветки?
Представь, что `main` — это главная книга проекта. Ветка — это твоя черновая копия, где ты делаешь изменения. Когда доделал — вливаешь обратно в главную книгу.

### Зачем нужны ветки?
- Чтобы твой недописанный код не сломал рабочую версию
- Чтобы несколько человек работали одновременно
- Чтобы можно было откатиться, если что-то пошло не так

### Типы веток

#### main (главная ветка)
- Защищённая ветка
- Только рабочий, протестированный код
- Нельзя пушить напрямую, только через Pull Request

#### feature/* (ветки с фичами)
- Для новых функций
- Примеры: `feature/login-page`, `feature/user-profile`

#### fix/* (ветки с багфиксами)
- Для исправления багов
- Примеры: `fix/login-button-crash`, `fix/email-validation`

#### refactor/* (рефакторинг)
- Для улучшения кода
- Примеры: `refactor/api-calls`, `refactor/auth-module`

### Команды для работы с ветками

```bash
# Посмотреть все ветки
git branch

# Посмотреть все ветки (включая удалённые)
git branch -a

# Создать новую ветку
git branch feature/my-task

# Переключиться на ветку
git checkout feature/my-task

# Создать ветку и сразу переключиться (чаще используется)
git checkout -b feature/my-task

# Удалить локальную ветку (после мержа в main)
git branch -d feature/my-task

# Удалить ветку на GitHub
git push origin --delete feature/my-task
```

### Пример workflow с ветками

```bash
# 1. Убедись что на main
git checkout main

# 2. Забери последние изменения
git pull origin main

# 3. Создай ветку для задачи
git checkout -b feature/add-search

# 4. Работай, коммить
git add .
git commit -m "feat: добавил форму поиска"

# 5. Запуш ветку
git push origin feature/add-search

# 6. Создай Pull Request на GitHub
# 7. После мержа — удали ветку
git checkout main
git pull origin main
git branch -d feature/add-search
```

---

## 5. Работа с GitHub

### Основные понятия

**Repository (репозиторий)** — проект, где хранится код  
**Fork** — копия чужого репозитория к себе  
**Clone** — скачать репозиторий на компьютер  
**Commit** — сохранённое изменение  
**Branch** — ветка разработки  
**Pull Request (PR)** — запрос на добавление твоего кода в main  

### Как стать collaborator

1. Владелец репозитория идёт в: **Settings** → **Collaborators** → **Add people**
2. Вводит твой GitHub username или email
3. Тебе приходит приглашение на почту
4. Принимаешь приглашение
5. Теперь можешь пушить в репозиторий

### Как правильно копировать ссылку репозитория

На странице репозитория:
1. Жми зелёную кнопку **Code**
2. **⚠️ ВЫБЕРИ ВКЛАДКУ "SSH"** (не HTTPS!)
3. Скопируй ссылку вида `git@github.com:username/repo.git`

```
✅ ПРАВИЛЬНО:   git@github.com:username/repo.git
❌ НЕПРАВИЛЬНО: https://github.com/username/repo.git
```

### Интерфейс GitHub

#### Code (Код)
- Здесь весь код проекта
- Можно смотреть файлы, историю изменений
- Зелёная кнопка **Code** → копируешь URL для `git clone`

#### Pull Requests
- Список всех PR (твоих и чужих)
- Можно ревьювить код, оставлять комментарии
- Владелец мержит PR в main

#### Issues
- Баг-трекер и список задач
- Можно создавать задачи, назначать на людей

#### Actions
- CI/CD (автоматические проверки, деплой)
- Пока не юзаем, можно игнорировать

#### Settings (только для владельца)
- Настройки репозитория
- Добавление collaborators
- Защита веток

---

## 6. Pull Request и совместная работа

### Что такое Pull Request (PR)?
Это запрос на добавление твоего кода из твоей ветки в `main`. Владелец/команда проверяет код и решает: мержить или отправить на доработку.

### Как создать Pull Request

#### Шаг 1: Запушь свою ветку
```bash
git push origin feature/my-task
```

#### Шаг 2: Открой GitHub
- Иди в репозиторий
- Увидишь жёлтый баннер: **Compare & pull request** — жми
- Или: вкладка **Pull requests** → **New pull request**

#### Шаг 3: Заполни форму PR
**Title (заголовок):**  
Краткое описание что сделано
```
Добавил страницу логина
```

**Description (описание):**  
Что сделано подробнее, скриншоты если надо
```
- Создал форму с email и паролем
- Добавил валидацию
- Подключил к API

Скриншот:
[прикрепить скриншот]
```

**Reviewers:**  
Выбери кого назначить на проверку (владельца или тимлида)

**Assignees:**  
Обычно себя

**Labels:**  
Можно добавить тег `feature`, `bug`, `enhancement`

#### Шаг 4: Жми Create pull request

### Что происходит после PR?

1. **Ревьювер проверяет код**
   - Может оставить комментарии
   - Может попросить что-то поправить
   - Может одобрить (Approve)

2. **Если нужны правки:**
```bash
# Внеси правки в тех же файлах
git add .
git commit -m "fix: исправил замечания по ревью"
git push origin feature/my-task
```
PR автоматически обновится!

3. **Если всё ОК:**
   - Ревьювер жмёт **Merge pull request**
   - Твой код попадает в `main`
   - Ветку можно удалить (обычно GitHub предлагает сам)

### После мержа PR

```bash
# Переключись на main
git checkout main

# Забери обновлённый main с твоими изменениями
git pull origin main

# Удали свою локальную ветку
git branch -d feature/my-task

# Удали ветку на GitHub (если не удалил через интерфейс)
git push origin --delete feature/my-task
```

---

## 7. Конфликты

### Что такое конфликт?
Ты и коллега изменили одну и ту же строку кода. Git не знает, какую версию оставить — он просит тебя решить.

### Когда возникают конфликты?
- Во время `git pull` (кто-то изменил тот же файл)
- Во время мержа PR (изменения накладываются)

### Как решить конфликт?

#### Ситуация: делаешь `git pull` и видишь:
```
CONFLICT (content): Merge conflict in app.js
Automatic merge failed; fix conflicts and then commit the result.
```

#### Шаг 1: Открой файл с конфликтом
В файле увидишь:
```javascript
function getUser() {
<<<<<<< HEAD
  return "Твоя версия кода";
=======
  return "Версия из GitHub";
>>>>>>> origin/main
}
```

**HEAD** — твоя текущая версия  
**origin/main** — версия с GitHub

#### Шаг 2: Реши что оставить
Удали лишнее, оставь правильную версию:
```javascript
function getUser() {
  return "Версия из GitHub"; // или твоя, или объедини обе
}
```

#### Шаг 3: Сохрани файл

#### Шаг 4: Добавь и закоммить
```bash
git add app.js
git commit -m "fix: разрешил конфликт в app.js"
git push origin feature/my-task
```

### Как избежать конфликтов?
- ✅ Чаще делай `git pull origin main` перед работой
- ✅ Коммить часто, не копи изменения
- ✅ Договоритесь кто в каких файлах работает
- ✅ Делай маленькие PR (не по 500 файлов)

---

## 8. .gitignore

### Что это?
Файл `.gitignore` указывает Git, какие файлы НЕ нужно отслеживать и пушить на GitHub.

### Что НЕ пушить?
- **Зависимости:** `node_modules/`, `venv/`, `vendor/`
- **Секреты:** `.env`, `config.local.js`, API ключи
- **Билды:** `dist/`, `build/`, `*.exe`
- **IDE настройки:** `.idea/`, `.vscode/`, `*.swp`
- **OS файлы:** `.DS_Store`, `Thumbs.db`
- **Логи:** `*.log`, `logs/`

### Как создать .gitignore

#### Шаг 1: Создай файл в корне проекта
```bash
touch .gitignore
```

#### Шаг 2: Добавь нужные правила
Пример для Node.js проекта:
```
# Зависимости
node_modules/
package-lock.json

# Environment переменные
.env
.env.local

# Билды
dist/
build/

# Логи
*.log
logs/

# IDE
.vscode/
.idea/

# OS файлы
.DS_Store
Thumbs.db
```

Пример для Python проекта:
```
# Virtual environment
venv/
env/
.venv/

# Python кеш
__pycache__/
*.pyc
*.pyo

# Environment
.env

# IDE
.idea/
.vscode/
```

#### Шаг 3: Закоммить .gitignore
```bash
git add .gitignore
git commit -m "chore: добавил .gitignore"
git push origin main
```

### Полезные ресурсы
- https://gitignore.io — генератор .gitignore для разных языков
- https://github.com/github/gitignore — готовые шаблоны

---

## 9. Шпаргалка команд

### Настройка (один раз)
```bash
git config --global user.name "Имя"
git config --global user.email "email@example.com"
```

### Начало работы
```bash
git clone git@github.com:user/repo.git  # Скачать репозиторий (SSH!)
cd project                              # Перейти в папку
git remote -v                           # Проверить что используется SSH
```

### Каждодневная работа
```bash
git status                   # Проверить статус
git pull origin main         # Забрать изменения с GitHub
git checkout -b feature/task # Создать ветку
git add .                    # Добавить все файлы
git add file.js              # Добавить один файл
git commit -m "feat: что сделал" # Закоммитить
git push origin feature/task # Запушить ветку
```

### Ветки
```bash
git branch                   # Список веток
git checkout main            # Переключиться на main
git checkout -b new-branch   # Создать и переключиться
git branch -d old-branch     # Удалить локальную ветку
git push origin --delete old # Удалить ветку на GitHub
```

### Полезные команды
```bash
git log                      # История коммитов
git log --oneline            # История кратко
git diff                     # Посмотреть изменения
git reset HEAD file.js       # Убрать файл из staging
git checkout -- file.js      # Откатить изменения в файле
git stash                    # Спрятать изменения
git stash pop                # Вернуть спрятанное
```

### Если что-то пошло не так
```bash
git reset --soft HEAD~1      # Отменить последний коммит (сохранить изменения)
git reset --hard HEAD~1      # Отменить последний коммит (удалить изменения)
git reflog                   # История всех действий (для восстановления)
```

---

## 🎯 Основной workflow (кратко)

```bash
# 1. Начало работы
git checkout main
git pull origin main
git checkout -b feature/my-task

# 2. Работаешь над кодом...

# 3. Сохраняешь прогресс
git add .
git commit -m "feat: сделал X"

# 4. Отправляешь на GitHub
git push origin feature/my-task

# 5. Создаёшь Pull Request на GitHub

# 6. После мержа PR
git checkout main
git pull origin main
git branch -d feature/my-task
```

---

## ❓ FAQ

**Q: Не могу запушить, пишет "Permission denied (publickey)". Что делать?**  
A: Скорее всего проблема с SSH. Проверь:
```bash
# 1. Проверь используется ли SSH
git remote -v
# Должно быть git@github.com, а не https://

# 2. Если HTTPS — поменяй на SSH
git remote set-url origin git@github.com:username/repo.git

# 3. Проверь SSH подключение
ssh -T git@github.com
# Должно: "Hi username! You've successfully authenticated"
```

**Q: Забыл сделать pull перед работой, теперь конфликты. Что делать?**  
A: 
```bash
git stash              # Спрячь свои изменения
git pull origin main   # Забери с GitHub
git stash pop          # Верни свои изменения
# Реши конфликты если есть
```

**Q: Случайно закоммитил в main вместо своей ветки. Что делать?**  
A:
```bash
git reset --soft HEAD~1  # Отменить коммит, оставить изменения
git checkout -b feature/correct-branch  # Создать правильную ветку
git add .
git commit -m "feat: правильный коммит"
```

**Q: Запушил в main напрямую (обошёл защиту). Как откатить?**  
A: Срочно пиши владельцу репозитория! Он сделает revert.

**Q: Удалил важную ветку случайно. Можно вернуть?**  
A: Да, если не прошло много времени:
```bash
git reflog           # Найди последний коммит ветки
git checkout -b restored-branch <commit-hash>
```

**Q: Как посмотреть что изменилось в чужом PR?**  
A: GitHub → Pull requests → выбери PR → вкладка **Files changed**

**Q: Можно редактировать коммит, который уже запушил?**  
A: Лучше не надо (сломаешь историю). Сделай новый коммит с правками.

---

## 📱 GitHub Mobile — для ревью на ходу

1. Скачай **GitHub Mobile** (iOS/Android)
2. Войди в аккаунт
3. **Settings** → **Notifications** → включи:
   - Pull requests
   - Pull request reviews
   - Comments
4. Теперь будут приходить пуши, можешь комментить и апрувить PR прямо с телефона

---

## ✅ Чеклист для каждой задачи

- [ ] Забрал последние изменения: `git pull origin main`
- [ ] Создал ветку: `git checkout -b feature/task-name`
- [ ] Делаю коммиты по мере работы
- [ ] Коммиты осмысленные: `feat:`, `fix:`, etc.
- [ ] Запушил ветку: `git push origin feature/task-name`
- [ ] Создал Pull Request на GitHub
- [ ] Назначил ревьювера
- [ ] Исправил замечания (если есть)
- [ ] PR смержен
- [ ] Вернулся на main: `git checkout main && git pull origin main`
- [ ] Удалил старую ветку: `git branch -d feature/task-name`

---

## 🆘 Полезные ссылки

- **Официальная документация:** https://git-scm.com/doc
- **GitHub Docs:** https://docs.github.com
- **Интерактивный тренажёр:** https://learngitbranching.js.org
- **Шпаргалка команд:** https://training.github.com/downloads/github-git-cheat-sheet.pdf
- **Генератор .gitignore:** https://gitignore.io

---

**Успехов в работе! Если что-то непонятно — спрашивай в общем чате. Мы все когда-то начинали! 🚀**
